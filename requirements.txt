# ── Core ──────────────────────────────────────────────────────────────
torch>=2.1
transformers>=4.35,<5
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
seqeval>=1.2.2
pyyaml>=6.0
tqdm>=4.66
requests>=2.31

# ── Morphological segmentation ────────────────────────────────────────
# Morfessor: unsupervised, pure-python, fully reproducible (primary).
morfessor>=2.0.6
# Zeyrek: pure-python Turkish morphological analyzer (Zemberek port, optional).
# zeyrek>=0.1.3

# ── Optional ──────────────────────────────────────────────────────────
# datasets>=2.14   # alternative HuggingFace loading path (not required)
# matplotlib>=3.7  # only for plotting figures in the paper
